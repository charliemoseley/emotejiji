<section id="emoticon-display">
  <h1>available tags</h1>
  <ul>
    <% @tags.each do |tag| %>
      <li>
        <a ng-click="addTag(tag)"><%= tag %></a>
      </li>
    <% end %>
  </ul>

  <a id="close-popover" href="{{closePopoverUrl()}}">X</a>
</section>